import { GameSetup } from '../types/types';

export const demoGameData:GameSetup = {
  words: [{offset: 0, text: 'cat'}, {offset: 0, text: 'dog'}],
  pathString: 'godcat',
  startWord: 1,
  startLetter: 2,
  theme: 'Furry friends'
};